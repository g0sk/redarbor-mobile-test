import React from 'react';
import { View, Text } from 'components';
import type { HeroDetailsScreenProps } from 'types';

export const HeroeDetails: React.FC<HeroDetailsScreenProps> = () => {
	return (
		<View>
			<Text>Heroe Details</Text>
		</View>
	);
};
